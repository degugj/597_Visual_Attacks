def color_forge_attack(inFrame, isVerPresent):
     # takes video frame and returns after color manipulation. isVerPresent determines how the pixels can be replaced (ie can't just swap colors with IFC)
     return inFrame
